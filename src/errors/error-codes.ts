export enum ErrorCode {

    // Bad Request
    USER_ALREADY_EXIST = 1001,
    INCORRECT_PASSWORD = 1003,

    // Not Found
    USER_NOT_FOUND = 2001,

    // Internal
    INTERNAL_EXCEPTION = 3001,
}